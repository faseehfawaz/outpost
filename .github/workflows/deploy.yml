name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build and Push Docker image
        run: |
          echo "Building image"
          # docker build -t ghcr.io/${{ github.repository }}/pkintel:latest .
          # docker push ghcr.io/${{ github.repository }}/pkintel:latest
      - name: SSH Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            echo "Deploying..."
            # docker pull ghcr.io/...
            # docker compose up -d
