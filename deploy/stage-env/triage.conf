[Service]
MemoryMax=8G
MemoryHigh=6G
Environment=OUTPOST_METRICS_PORT=9101
Environment=OUTPOST_INTERVAL=30
# Chromium profile + cache live on tmpfs (PKINTEL_RENDER_TMPFS_DIR).
ReadWritePaths=/dev/shm/outpost-render
