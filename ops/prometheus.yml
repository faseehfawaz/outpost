global:
  scrape_interval: 15s
  external_labels:
    environment: production

scrape_configs:
  - job_name: 'pkintel_api'
    static_configs:
      - targets: ['api:8000']

  - job_name: 'node_exporter'
    static_configs:
      - targets: ['localhost:9100']
